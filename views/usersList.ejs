<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title><%= title %></title>
    </head>
    <body>
        <% if (locals.users) { %>
            <ul>
                <% users.map((user) => { %>
                    <li><%= user.username %></li>
                <% }) %>
            </ul>
        <% } %>
    </body>
</html>